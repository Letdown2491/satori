// NIP-84 Highlights: a kind:9802 event signalling a passage someone found valuable. `.content` is the
// highlighted text; `r`/`a`/`e` tag the source (web url / nostr addressable / nostr event); `context` the
// surrounding sentence; `p` the original author(s). Rendered by the dedicated handler (manifest/highlight.ts).

export const KIND_HIGHLIGHT = 9802;
